<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.seata.sample.dao.StorageDao" >
  <resultMap id="BaseResultMap" type="io.seata.sample.entity.Storage" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="product_id" property="productId" jdbcType="BIGINT" />
    <result column="total" property="total" jdbcType="INTEGER" />
    <result column="used" property="used" jdbcType="INTEGER" />
    <result column="residue" property="residue" jdbcType="INTEGER" />
  </resultMap>
  <update id="decrease">
    UPDATE storage SET used = used + #{count},residue = residue - #{count} WHERE product_id = #{productId}
  </update>

  <select id="getById" resultType="java.lang.Integer">
    select  used from storage where product_id = #{productId};
  </select>

  <update id="decrease2">
    UPDATE storage SET used = used + #{count},residue = residue - #{count} WHERE product_id = 2
  </update>

  <update id="decrease3">
    UPDATE storage SET used = used + #{count},residue = residue - #{count} WHERE product_id = 3
  </update>

  <update id="decrease4">
    UPDATE storage SET used = used + #{count},residue = residue - #{count} WHERE product_id = 4
  </update>

  <update id="decrease5">
    UPDATE apple SET count = count - #{count} WHERE id = 1
  </update>
</mapper>

